First, checkout Ghost into `./ghost` directory:

```
git clone --depth 1 https://github.com/TryGhost/Ghost.git ghost
```

To test, run:

```bash
make clean run
```

To upload the theme, use `theme.zip`:

```bash
make clean zip
```

